<!DOCTYPE html>
<html lang="pt-BR">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width,initial-scale=1.0,viewport-fit=cover">
<title>Burguer Gold — Admin</title>

<script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2"></script>

<style>
*{box-sizing:border-box}

html{scroll-behavior:smooth}

body{
margin:0;
font-family:Arial,Helvetica,sans-serif;
background:#080808;
color:#fff
}

button,input,select{
font:inherit
}

button{
cursor:pointer
}

.hidden{
display:none!important
}

.gold{
color:#d4af37
}

/* LOGIN */

.login-screen{
min-height:100vh;
display:flex;
align-items:center;
justify-content:center;
padding:20px;
background:
radial-gradient(circle,#3c300d 0%,#151515 35%,#050505 80%)
}

.login-box{
width:min(430px,100%);
background:#111;
border:1px solid #333;
border-radius:22px;
padding:28px;
box-shadow:0 20px 60px #000
}

.logo{
text-align:center;
font-size:30px;
font-weight:900;
color:#d4af37;
margin-bottom:8px
}

.login-subtitle{
text-align:center;
color:#999;
margin-bottom:25px
}

.form-group{
margin-top:16px
}

.form-group label{
display:block;
font-weight:bold;
margin-bottom:7px
}

.form-group input,
.form-group select{
width:100%;
padding:13px;
background:#1b1b1b;
border:1px solid #3a3a3a;
border-radius:10px;
color:#fff;
outline:none
}

.btn{
border:0;
border-radius:10px;
padding:12px 16px;
font-weight:900
}

.btn-gold{
background:#d4af37;
color:#000
}

.btn-dark{
background:#292929;
color:#fff
}

.btn-red{
background:#5c1717;
color:#fff
}

.login-button{
width:100%;
margin-top:22px;
padding:15px;
border:0;
border-radius:12px;
background:#d4af37;
color:#000;
font-weight:900;
font-size:17px
}

.error{
margin-top:15px;
padding:12px;
background:#3a1111;
border:1px solid #8b2929;
border-radius:10px;
color:#ff8d8d;
text-align:center
}

/* HEADER */

.admin-header{
position:sticky;
top:0;
z-index:1000;
background:#090909;
border-bottom:1px solid #292929;
padding:14px 18px
}

.header-inner{
max-width:1200px;
margin:auto;
display:flex;
align-items:center;
justify-content:space-between;
gap:15px
}

.header-logo{
color:#d4af37;
font-size:23px;
font-weight:900
}

.header-actions{
display:flex;
gap:8px;
flex-wrap:wrap
}

/* MAIN */

main{
max-width:1200px;
margin:auto;
padding:25px 18px 60px
}

.welcome{
margin-bottom:22px
}

.welcome h1{
margin:0 0 7px;
font-size:30px
}

.welcome p{
margin:0;
color:#999
}

/* TABS */

.tabs{
display:flex;
gap:8px;
overflow-x:auto;
padding-bottom:8px;
margin-bottom:20px
}

.tab{
flex:none;
background:#171717;
border:1px solid #333;
color:#fff;
border-radius:12px;
padding:11px 17px;
font-weight:900
}

.tab.active{
background:#d4af37;
color:#000;
border-color:#d4af37
}

/* PANELS */

.panel{
display:none
}

.panel.active{
display:block
}

.section-box{
background:#111;
border:1px solid #292929;
border-radius:17px;
padding:20px;
margin-bottom:18px
}

.section-title{
display:flex;
justify-content:space-between;
align-items:center;
gap:10px;
flex-wrap:wrap;
margin-bottom:18px
}

.section-title h2{
margin:0;
color:#d4af37;
font-size:22px
}

/* ORDERS */

.orders{
display:grid;
gap:15px
}

.order{
background:#161616;
border:1px solid #303030;
border-radius:15px;
padding:18px
}

.order-top{
display:flex;
justify-content:space-between;
align-items:flex-start;
gap:12px;
flex-wrap:wrap
}

.order-number{
font-size:20px;
font-weight:900;
color:#d4af37
}

.order-date{
color:#888;
font-size:13px;
margin-top:4px
}

.status-select{
background:#222;
border:1px solid #444;
color:#fff;
padding:9px;
border-radius:9px;
font-weight:bold
}

.customer{
margin-top:15px;
line-height:1.6
}

.items{
margin-top:15px;
padding:14px;
background:#0d0d0d;
border-radius:10px
}

.item-line{
display:flex;
justify-content:space-between;
gap:10px;
padding:6px 0;
border-bottom:1px solid #222
}

.item-line:last-child{
border-bottom:0
}

.order-info{
margin-top:14px;
color:#bbb;
line-height:1.7
}

.order-total{
display:flex;
justify-content:space-between;
margin-top:15px;
padding-top:15px;
border-top:1px solid #333;
font-size:20px;
font-weight:900
}

.empty{
background:#161616;
border:1px solid #292929;
border-radius:14px;
padding:30px;
text-align:center;
color:#888
}

/* PRODUCTS */

.form-grid{
display:grid;
grid-template-columns:repeat(2,1fr);
gap:14px
}

.full{
grid-column:1/-1
}

.products-list,
.zones-list,
.hours-list{
display:grid;
gap:12px
}

.product-row,
.zone-row{
background:#171717;
border:1px solid #303030;
border-radius:13px;
padding:15px
}

.row-main{
display:flex;
justify-content:space-between;
gap:15px;
align-items:center;
flex-wrap:wrap
}

.row-name{
font-weight:900;
font-size:18px
}

.row-description{
color:#888;
font-size:13px;
margin-top:4px
}

.row-price{
color:#d4af37;
font-weight:900;
margin-top:5px
}

.row-actions{
display:flex;
gap:7px;
flex-wrap:wrap
}

.badge{
display:inline-block;
padding:5px 9px;
border-radius:20px;
font-size:12px;
font-weight:900;
margin-top:6px
}

.badge-on{
background:#123b25;
color:#58e893
}

.badge-off{
background:#3d1515;
color:#ff7777
}

/* HOURS */

.hour-row{
background:#171717;
border:1px solid #303030;
border-radius:13px;
padding:15px;
display:grid;
grid-template-columns:130px 1fr 1fr auto;
gap:12px;
align-items:center
}

.day-name{
font-weight:900
}

.hour-row input[type="time"]{
background:#222;
border:1px solid #444;
color:#fff;
padding:10px;
border-radius:9px
}

.open-check{
display:flex;
align-items:center;
gap:7px;
font-weight:bold
}

.save-hours{
margin-top:15px
}

/* RESPONSIVE */

@media(max-width:700px){

.form-grid{
grid-template-columns:1fr
}

.full{
grid-column:auto
}

.hour-row{
grid-template-columns:1fr 1fr
}

.day-name{
grid-column:1/-1
}

.open-check{
grid-column:1/-1
}

.header-inner{
align-items:flex-start;
flex-direction:column
}

.header-actions{
width:100%
}

.header-actions button{
flex:1
}

}
</style>
</head>

<body>


<!-- =========================================
     LOGIN
========================================= -->

<div id="loginScreen" class="login-screen">

<div class="login-box">

<div class="logo">
🍔 BURGUER GOLD
</div>

<div class="login-subtitle">
Painel administrativo
</div>

<form id="loginForm">

<div class="form-group">

<label>E-mail</label>

<input
id="loginEmail"
type="email"
value="goldburguer89@gmail.com"
required
>

</div>

<div class="form-group">

<label>Senha</label>

<input
id="loginPassword"
type="password"
placeholder="Digite sua senha"
required
>

</div>

<div
id="loginError"
class="error hidden">
</div>

<button
class="login-button"
id="loginButton"
type="submit">

ENTRAR NO PAINEL

</button>

</form>

</div>

</div>


<!-- =========================================
     ADMIN
========================================= -->

<div id="adminScreen" class="hidden">

<header class="admin-header">

<div class="header-inner">

<div class="header-logo">
🍔 BURGUER GOLD — ADMIN
</div>

<div class="header-actions">

<button
class="btn btn-dark"
onclick="window.location.href='index.html'">

🛒 Ver loja

</button>

<button
class="btn btn-red"
onclick="logout()">

Sair

</button>

</div>

</div>

</header>


<main>

<div class="welcome">

<h1>
Painel de controle
</h1>

<p>
Gerencie pedidos, produtos, bairros e horários da Burguer Gold.
</p>

</div>


<!-- TABS -->

<div class="tabs">

<button
class="tab active"
onclick="showTab('orders',this)">

📦 Pedidos

</button>

<button
class="tab"
onclick="showTab('products',this)">

🍔 Produtos

</button>

<button
class="tab"
onclick="showTab('zones',this)">

🛵 Bairros / Taxas

</button>

<button
class="tab"
onclick="showTab('hours',this)">

🕐 Horários

</button>

</div>


<!-- =========================================
     PEDIDOS
========================================= -->

<section
id="ordersPanel"
class="panel active">

<div class="section-box">

<div class="section-title">

<h2>
📦 Pedidos recebidos
</h2>

<button
class="btn btn-gold"
onclick="loadOrders()">

🔄 Atualizar

</button>

</div>

<div
id="ordersList"
class="orders">

<div class="empty">
Carregando pedidos...
</div>

</div>

</div>

</section>


<!-- =========================================
     PRODUTOS
========================================= -->

<section
id="productsPanel"
class="panel">

<div class="section-box">

<div class="section-title">

<h2>
➕ Adicionar produto
</h2>

</div>

<form id="productForm">

<div class="form-grid">

<div class="form-group">

<label>Nome</label>

<input
id="productName"
required
placeholder="Ex.: X-Salada"
>

</div>

<div class="form-group">

<label>Preço</label>

<input
id="productPrice"
type="number"
step="0.01"
min="0"
required
placeholder="25.90"
>

</div>

<div class="form-group">

<label>Categoria</label>

<input
id="productCategory"
value="Lanches"
required
placeholder="Lanches"
>

</div>

<div class="form-group full">

<label>Descrição</label>

<input
id="productDescription"
placeholder="Descrição do produto"
>

</div>

</div>

<button
class="btn btn-gold"
type="submit"
style="margin-top:15px">

ADICIONAR PRODUTO

</button>

</form>

</div>


<div class="section-box">

<div class="section-title">

<h2>
🍔 Produtos cadastrados
</h2>

<button
class="btn btn-dark"
onclick="loadProducts()">

🔄 Atualizar

</button>

</div>

<div
id="productsList"
class="products-list">

<div class="empty">
Carregando produtos...
</div>

</div>

</div>

</section>


<!-- =========================================
     BAIRROS
========================================= -->

<section
id="zonesPanel"
class="panel">

<div class="section-box">

<div class="section-title">

<h2>
➕ Adicionar bairro
</h2>

</div>

<form id="zoneForm">

<div class="form-grid">

<div class="form-group">

<label>Bairro</label>

<input
id="zoneNeighborhood"
required
placeholder="Ex.: Retiro"
>

</div>

<div class="form-group">

<label>Taxa de entrega</label>

<input
id="zoneFee"
type="number"
step="0.01"
min="0"
required
placeholder="5.00"
>

</div>

</div>

<button
class="btn btn-gold"
type="submit"
style="margin-top:15px">

ADICIONAR BAIRRO

</button>

</form>

</div>


<div class="section-box">

<div class="section-title">

<h2>
🛵 Bairros cadastrados
</h2>

<button
class="btn btn-dark"
onclick="loadZones()">

🔄 Atualizar

</button>

</div>

<div
id="zonesList"
class="zones-list">

<div class="empty">
Carregando bairros...
</div>

</div>

</div>

</section>


<!-- =========================================
     HORÁRIOS
========================================= -->

<section
id="hoursPanel"
class="panel">

<div class="section-box">

<div class="section-title">

<h2>
🕐 Horários de funcionamento
</h2>

<button
class="btn btn-gold"
onclick="loadHours()">

🔄 Recarregar

</button>

</div>

<div
id="hoursListAdmin"
class="hours-list">

<div class="empty">
Carregando horários...
</div>

</div>

<button
class="btn btn-gold save-hours"
onclick="saveHours()">

💾 SALVAR HORÁRIOS

</button>

</div>

</section>

</main>

</div>


<script>

/* ==========================================
   CONFIGURAÇÃO
========================================== */

const SUPABASE_URL =
"https://tliifpjfrjuxkjqyrvnw.supabase.co";

const SUPABASE_KEY =
"sb_publishable_Hh-QYqm0beaP_HBhU5i3yg_BFYK_3tj";

const supabaseClient =
window.supabase.createClient(
SUPABASE_URL,
SUPABASE_KEY
);


/* ==========================================
   VARIÁVEIS
========================================== */

let currentHours = [];


/* ==========================================
   FUNÇÕES GERAIS
========================================== */

function money(value){

return Number(value || 0).toLocaleString(
"pt-BR",
{
style:"currency",
currency:"BRL"
}
);

}


function escapeHtml(text){

return String(text ?? "")
.replaceAll("&","&amp;")
.replaceAll("<","&lt;")
.replaceAll(">","&gt;")
.replaceAll('"',"&quot;")
.replaceAll("'","&#039;");

}


/* ==========================================
   LOGIN
========================================== */

document
.getElementById("loginForm")
.addEventListener(
"submit",
async function(event){

event.preventDefault();

const email =
document.getElementById("loginEmail")
.value
.trim();

const password =
document.getElementById("loginPassword")
.value;

const button =
document.getElementById("loginButton");

const errorBox =
document.getElementById("loginError");

errorBox.classList.add("hidden");

button.disabled = true;

button.textContent =
"ENTRANDO...";

const {data,error} =
await supabaseClient.auth.signInWithPassword({
email,
password
});

button.disabled = false;

button.textContent =
"ENTRAR NO PAINEL";

if(error){

console.error(error);

errorBox.textContent =
"Não foi possível entrar. Confira o e-mail e a senha.";

errorBox.classList.remove("hidden");

return;
}

showAdmin();

});


async function checkSession(){

const {
data:{
session
}
} =
await supabaseClient.auth.getSession();

if(session){

showAdmin();

}else{

showLogin();

}

}


function showLogin(){

document
.getElementById("loginScreen")
.classList.remove("hidden");

document
.getElementById("adminScreen")
.classList.add("hidden");

}


async function showAdmin(){

document
.getElementById("loginScreen")
.classList.add("hidden");

document
.getElementById("adminScreen")
.classList.remove("hidden");

await loadOrders();
await loadProducts();
await loadZones();
await loadHours();

}


async function logout(){

await supabaseClient.auth.signOut();

location.reload();

}


/* ==========================================
   ABAS
========================================== */

function showTab(tab,button){

document
.querySelectorAll(".tab")
.forEach(
item =>
item.classList.remove("active")
);

button.classList.add("active");

document
.querySelectorAll(".panel")
.forEach(
panel =>
panel.classList.remove("active")
);

document
.getElementById(tab + "Panel")
.classList.add("active");

}


/* ==========================================
   PEDIDOS
========================================== */

async function loadOrders(){

const container =
document.getElementById("ordersList");

container.innerHTML =
`
<div class="empty">
Carregando pedidos...
</div>
`;

const {
data,
error
} =
await supabaseClient
.from("orders")
.select(`
*,
order_items(*)
`)
.order("created_at",{ascending:false});

if(error){

console.error(error);

container.innerHTML =
`
<div class="empty">
Erro ao carregar os pedidos.
<br><br>
${escapeHtml(error.message)}
</div>
`;

return;
}

if(!data || !data.length){

container.innerHTML =
`
<div class="empty">
📦 Nenhum pedido recebido ainda.
</div>
`;

return;
}

container.innerHTML =
data.map(order => {

const date =
new Date(order.created_at)
.toLocaleString(
"pt-BR"
);

const items =
order.order_items || [];

const itemsHtml =
items.map(item => {

return `
<div class="item-line">

<span>
${item.qty}x
${escapeHtml(item.name)}
</span>

<strong>
${money(
Number(item.price) *
Number(item.qty)
)}
</strong>

</div>
`;

}).join("");

return `
<div class="order">

<div class="order-top">

<div>

<div class="order-number">
Pedido #${order.id}
</div>

<div class="order-date">
${date}
</div>

</div>

<select
class="status-select"
onchange="updateOrderStatus(${order.id},this.value)">

<option
value="RECEBIDO"
${order.status==="RECEBIDO"?"selected":""}>
RECEBIDO
</option>

<option
value="EM_PREPARO"
${order.status==="EM_PREPARO"?"selected":""}>
EM PREPARO
</option>

<option
value="SAIU_ENTREGA"
${order.status==="SAIU_ENTREGA"?"selected":""}>
SAIU PARA ENTREGA
</option>

<option
value="PRONTO"
${order.status==="PRONTO"?"selected":""}>
PRONTO
</option>

<option
value="FINALIZADO"
${order.status==="FINALIZADO"?"selected":""}>
FINALIZADO
</option>

<option
value="CANCELADO"
${order.status==="CANCELADO"?"selected":""}>
CANCELADO
</option>

</select>

</div>


<div class="customer">

<strong>
👤 ${escapeHtml(order.customer_name)}
</strong>

</div>


<div class="items">

<strong>
Itens do pedido
</strong>

${itemsHtml}

</div>


<div class="order-info">

<strong>
📦 Recebimento:
</strong>

${
order.delivery
? "🛵 Entrega"
: "🏪 Retirada"
}

<br>

${
order.delivery
?
`
<strong>📍 Endereço:</strong>
${escapeHtml(order.address)}
<br>
<strong>🏘️ Bairro:</strong>
${escapeHtml(order.neighborhood)}
<br>
<strong>🛵 Taxa:</strong>
${money(order.delivery_fee)}
`
:
""
}

<br>

<strong>
💳 Pagamento:
</strong>

${escapeHtml(order.payment)}

</div>


<div class="order-total">

<span>
TOTAL
</span>

<span class="gold">
${money(order.total)}
</span>

</div>

</div>
`;

}).join("");

}


async function updateOrderStatus(id,status){

const {error} =
await supabaseClient
.from("orders")
.update({
status
})
.eq("id",id);

if(error){

console.error(error);

alert(
"Não foi possível atualizar o status."
);

return;
}

loadOrders();

}


/* ==========================================
   PRODUTOS
========================================== */

async function loadProducts(){

const container =
document.getElementById("productsList");

const {
data,
error
} =
await supabaseClient
.from("products")
.select("*")
.order("id");

if(error){

console.error(error);

container.innerHTML =
`
<div class="empty">
Erro ao carregar produtos.
</div>
`;

return;
}

if(!data || !data.length){

container.innerHTML =
`
<div class="empty">
Nenhum produto cadastrado.
</div>
`;

return;
}

container.innerHTML =
data.map(product => {

return `
<div class="product-row">

<div class="row-main">

<div>

<div class="row-name">
${escapeHtml(product.name)}
</div>

<div class="row-description">
${escapeHtml(product.description || "")}
</div>

<div class="row-price">
${money(product.price)}
</div>

<span class="badge ${
product.active
? "badge-on"
: "badge-off"
}">
${
product.active
? "ATIVO"
: "INATIVO"
}
</span>

</div>


<div class="row-actions">

<button
class="btn btn-dark"
onclick="editProduct(${product.id})">

✏️ Editar

</button>

<button
class="btn ${
product.active
? "btn-red"
: "btn-gold"
}"
onclick="toggleProduct(${product.id},${product.active})">

${
product.active
? "Desativar"
: "Ativar"
}

</button>

</div>

</div>

</div>
`;

}).join("");

}


async function addProduct(event){

event.preventDefault();

const name =
document.getElementById("productName")
.value.trim();

const description =
document.getElementById("productDescription")
.value.trim();

const price =
Number(
document.getElementById("productPrice")
.value
);

const category =
document.getElementById("productCategory")
.value.trim();

if(!name || !category || price < 0){

alert("Preencha os dados corretamente.");

return;
}

const {error} =
await supabaseClient
.from("products")
.insert({
name,
description,
price,
category,
active:true
});

if(error){

console.error(error);

alert(
"Erro ao adicionar produto: " +
error.message
);

return;
}

document
.getElementById("productForm")
.reset();

document
.getElementById("productCategory")
.value =
"Lanches";

await loadProducts();

alert("Produto adicionado com sucesso!");


}


document
.getElementById("productForm")
.addEventListener(
"submit",
addProduct
);


async function toggleProduct(id,active){

const {error} =
await supabaseClient
.from("products")
.update({
active:!active
})
.eq("id",id);

if(error){

console.error(error);

alert(
"Não foi possível alterar o produto."
);

return;
}

loadProducts();

}


async function editProduct(id){

const {
data,
error
} =
await supabaseClient
.from("products")
.select("*")
.eq("id",id)
.single();

if(error || !data){

alert("Produto não encontrado.");

return;
}

const name =
prompt(
"Nome do produto:",
data.name
);

if(name === null) return;

const priceText =
prompt(
"Preço:",
data.price
);

if(priceText === null) return;

const price =
Number(
priceText.replace(",",".")
);

if(isNaN(price) || price < 0){

alert("Preço inválido.");

return;
}

const category =
prompt(
"Categoria:",
data.category
);

if(category === null) return;

const description =
prompt(
"Descrição:",
data.description || ""
);

if(description === null) return;

const {error:updateError} =
await supabaseClient
.from("products")
.update({
name:name.trim(),
price,
category:category.trim(),
description:description.trim()
})
.eq("id",id);

if(updateError){

console.error(updateError);

alert(
"Não foi possível editar o produto."
);

return;
}

loadProducts();

}


/* ==========================================
   BAIRROS
========================================== */

async function loadZones(){

const container =
document.getElementById("zonesList");

const {
data,
error
} =
await supabaseClient
.from("delivery_zones")
.select("*")
.order("neighborhood");

if(error){

console.error(error);

container.innerHTML =
`
<div class="empty">
Erro ao carregar bairros.
</div>
`;

return;
}

if(!data || !data.length){

container.innerHTML =
`
<div class="empty">
Nenhum bairro cadastrado.
</div>
`;

return;
}

container.innerHTML =
data.map(zone => {

return `
<div class="zone-row">

<div class="row-main">

<div>

<div class="row-name">
${escapeHtml(zone.neighborhood)}
</div>

<div class="row-price">
Taxa: ${money(zone.fee)}
</div>

<span class="badge ${
zone.active
? "badge-on"
: "badge-off"
}">
${
zone.active
? "ATIVO"
: "INATIVO"
}
</span>

</div>

<div class="row-actions">

<button
class="btn btn-dark"
onclick="editZone(${zone.id})">

✏️ Editar

</button>

<button
class="btn ${
zone.active
? "btn-red"
: "btn-gold"
}"
onclick="toggleZone(${zone.id},${zone.active})">

${
zone.active
? "Desativar"
: "Ativar"
}

</button>

</div>

</div>

</div>
`;

}).join("");

}


document
.getElementById("zoneForm")
.addEventListener(
"submit",
async function(event){

event.preventDefault();

const neighborhood =
document
.getElementById("zoneNeighborhood")
.value
.trim();

const fee =
Number(
document.getElementById("zoneFee")
.value
);

if(!neighborhood || fee < 0){

alert(
"Preencha o bairro e a taxa corretamente."
);

return;
}

const {error} =
await supabaseClient
.from("delivery_zones")
.insert({
neighborhood,
fee,
active:true
});

if(error){

console.error(error);

alert(
"Erro ao adicionar bairro: " +
error.message
);

return;
}

document
.getElementById("zoneForm")
.reset();

await loadZones();

alert("Bairro adicionado com sucesso!");

});


async function toggleZone(id,active){

const {error} =
await supabaseClient
.from("delivery_zones")
.update({
active:!active
})
.eq("id",id);

if(error){

console.error(error);

alert(
"Não foi possível alterar o bairro."
);

return;
}

loadZones();

}


async function editZone(id){

const {
data,
error
} =
await supabaseClient
.from("delivery_zones")
.select("*")
.eq("id",id)
.single();

if(error || !data){

alert("Bairro não encontrado.");

return;
}

const neighborhood =
prompt(
"Bairro:",
data.neighborhood
);

if(neighborhood === null) return;

const feeText =
prompt(
"Taxa de entrega:",
data.fee
);

if(feeText === null) return;

const fee =
Number(
feeText.replace(",",".")
);

if(isNaN(fee) || fee < 0){

alert("Taxa inválida.");

return;
}

const {error:updateError} =
await supabaseClient
.from("delivery_zones")
.update({
neighborhood:neighborhood.trim(),
fee
})
.eq("id",id);

if(updateError){

console.error(updateError);

alert(
"Não foi possível editar o bairro."
);

return;
}

loadZones();

}


/* ==========================================
   HORÁRIOS
========================================== */

async function loadHours(){

const container =
document.getElementById("hoursListAdmin");

const {
data,
error
} =
await supabaseClient
.from("store_hours")
.select("*")
.order("day_of_week");

if(error){

console.error(error);

container.innerHTML =
`
<div class="empty">
Erro ao carregar horários.
<br><br>
${escapeHtml(error.message)}
</div>
`;

return;
}

currentHours = data || [];

if(!currentHours.length){

container.innerHTML =
`
<div class="empty">
Nenhum horário cadastrado.
</div>
`;

return;
}

container.innerHTML =
currentHours.map(day => {

const openTime =
day.open_time
? String(day.open_time).slice(0,5)
: "18:00";

const closeTime =
day.close_time
? String(day.close_time).slice(0,5)
: "23:00";

return `
<div
class="hour-row"
data-id="${day.id}">

<div class="day-name">
${escapeHtml(day.day_name)}
</div>

<input
class="open-time"
type="time"
value="${openTime}"
${day.is_open ? "" : "disabled"}
>

<input
class="close-time"
type="time"
value="${closeTime}"
${day.is_open ? "" : "disabled"}
>

<label class="open-check">

<input
class="day-open"
type="checkbox"
${day.is_open ? "checked" : ""}
onchange="toggleHourInputs(this)"
>

Aberto

</label>

</div>
`;

}).join("");

}


function toggleHourInputs(checkbox){

const row =
checkbox.closest(".hour-row");

const inputs =
row.querySelectorAll(
".open-time,.close-time"
);

inputs.forEach(input => {

input.disabled =
!checkbox.checked;

});

}


async function saveHours(){

const rows =
document.querySelectorAll(
".hour-row"
);

if(!rows.length){

alert("Nenhum horário para salvar.");

return;
}

for(const row of rows){

const id =
row.dataset.id;

const isOpen =
row.querySelector(".day-open")
.checked;

const openTime =
row.querySelector(".open-time")
.value;

const closeTime =
row.querySelector(".close-time")
.value;

const {error} =
await supabaseClient
.from("store_hours")
.update({

is_open:isOpen,

open_time:
isOpen
? openTime
: null,

close_time:
isOpen
? closeTime
: null

})
.eq("id",id);

if(error){

console.error(error);

alert(
"Erro ao salvar os horários: " +
error.message
);

return;
}

}

await loadHours();

alert(
"Horários salvos com sucesso! ✅"
);

}


/* ==========================================
   REALTIME DOS PEDIDOS
========================================== */

function startRealtime(){

supabaseClient
.channel("admin-orders")
.on(
"postgres_changes",
{
event:"INSERT",
schema:"public",
table:"orders"
},
payload => {

console.log(
"Novo pedido:",
payload.new
);

loadOrders();

})
.on(
"postgres_changes",
{
event:"UPDATE",
schema:"public",
table:"orders"
},
payload => {

loadOrders();

})
.subscribe();

}


/* ==========================================
   INICIAR
========================================== */

checkSession();

startRealtime();

</script>

</body>
</html>
